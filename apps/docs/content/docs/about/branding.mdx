---
title: "Branding"
description: "These resources exist to help you use Mesh's assets."
---
import Link from "next/link";
import Image from "next/image";

## Logo [!toc]

The Mesh logo is available in two color schemes: black and white. You can use the logo in either color scheme depending on your design needs. The logo is available in various sizes to suit different use cases.

<Cards className="mb-16 mt-10">
  <Card>
    <Image
      src="/logo-mesh/black/logo-mesh-black-512x512.png"
      width={500}
      height={250}
      alt="mesh-black-512"
      className="bg-white rounded"
    />

    <div className="flex flex-col items-center gap-2">
      <div className="font-bold text-lg">Download black logo:</div>
      <div className="flex flex-wrap justify-center items-center gap-2">
        <Link
          href={`/logo-mesh/black/logo-mesh-vector.svg`}
          className="text-blue-500 no-underline hover:underline leading-none"
        >
          SVG
        </Link>
        {["16x16", "32x32", "64x64", "128x128", "256x256", "512x512"].map((size) => (
          <Link
            key={size}
            href={`/logo-mesh/black/logo-mesh-black-${size}.png`}
            target="_blank"
            className="text-blue-500 no-underline hover:underline leading-none"
          >
            {size}
          </Link>
        ))}
      </div>
    </div>
  </Card>

  <Card>
    <Image
      src="/logo-mesh/white/logo-mesh-white-512x512.png"
      width={500}
      height={250}
      alt="mesh-white-512"
      className="bg-black rounded"
    />

    <div className="flex flex-col items-center gap-2">
      <div className="font-bold text-lg">Download white logo:</div>
      <div className="flex flex-wrap justify-center items-center gap-2">
        <Link
          href={`/logo-mesh/white/logo-mesh-vector.svg`}
          className="text-blue-500 no-underline hover:underline leading-none"
        >
          SVG
        </Link>
        {["16x16", "32x32", "64x64", "128x128", "256x256", "512x512"].map((size) => (
          <Link
            key={size}
            href={`/logo-mesh/white/logo-mesh-white-${size}.png`}
            target="_blank"
            className="text-blue-500 no-underline hover:underline leading-none"
          >
            {size}
          </Link>
        ))}
      </div>
    </div>
  </Card>
</Cards>

<Cards className="mb-16">
  <Card>
    <Image
      src="/logo-mesh/meshlogo-with-title-black.svg"
      width={500}
      height={250}
      alt="mesh-black-title-512"
      className="bg-white rounded mb-0"
    />

    <div className="flex items-center justify-center gap-2 font-bold text-lg">
      <div>Download black logo with title:</div>
      <Link
        href={`/logo-mesh/meshlogo-with-title-black.svg`}
        className="text-blue-500 no-underline hover:underline"
      >
        SVG
      </Link>
    </div>
  </Card>

  <Card>
    <Image
      src="/logo-mesh/meshlogo-with-title-white.svg"
      width={500}
      height={250}
      alt="mesh-white-title-512"
      className="bg-black rounded mb-0"
    />

    <div className="flex items-center justify-center gap-2 font-bold text-lg leading-none">
      <span>Download white logo with title:</span>
      <Link
        href={`/logo-mesh/meshlogo-with-title-white.svg`}
        className="text-blue-500 no-underline hover:underline"
      >
        SVG
      </Link>
    </div>
  </Card>
</Cards>


<Cards>
  <Card>
    <Image
      src="/logo-mesh/mesh.png"
      width={500}
      height={250}
      alt="mesh-background"
      className="rounded mb-0"
    />

    <div className="flex items-center justify-center gap-2 font-bold text-lg leading-none">
      <span>Download logo with background:</span>
      <Link
        href={`/logo-mesh/mesh.png`}
        className="text-blue-500 no-underline hover:underline"
      >
        <p>PNG</p>
      </Link>
    </div>
  </Card>
</Cards>